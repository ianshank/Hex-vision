"""Planning source package for deterministic Hex-vision project projections.

The package is intentionally independent of ``hexvision`` so planning data can be
validated before runtime modules are installed. ``roadmap_data`` is the canonical
source for the generated roadmap and backlog views.
"""
